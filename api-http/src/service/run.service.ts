import { SubmitDsaSchemaType } from "../schema/submission.schema";

export const runCode = async (data: SubmitDsaSchemaType) => {

    // const { code, language } = data;

    // const executionResult = 

    // return executionResult;
}